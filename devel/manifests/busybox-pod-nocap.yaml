apiVersion: v1
kind: Pod
metadata:
  name:   busybox-nocap
spec:
  containers:
    - name:    busybox
      image: busybox:latest
      args: [sh, -c, 'echo $UID && sleep 600']
      securityContext:
        runAsUser: 65534
        capabilities:
          drop:
            - CHOWN
            - DAC_OVERRIDE
            - FSETID
            - FOWNER
            - MKNOD
            - NET_RAW
            - SETGID
            - SETUID
            - SETFCAP
            - SETPCAP
            - NET_BIND_SERVICE
            - SYS_CHROOT
            - KILL
            - AUDIT_WRITE
